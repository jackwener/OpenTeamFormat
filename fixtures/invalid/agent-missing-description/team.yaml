apiVersion: openteamformat.org/v1alpha1
kind: OpenTeam
metadata:
  name: bad-team
  displayName: Bad Team
  description: Invalid because an agent reference is missing description.
spec:
  agents:
    - name: product-manager
      path: agents/product-manager/agent.yaml
  channels:
    - name: bad-team
      displayName: Bad Team
      description: Main channel.
      members:
        - ref: agent:product-manager
          role: member
